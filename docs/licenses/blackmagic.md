# Blackmagic Design — not redistributed

DaVinci Resolve, the Scripting API, Workflow Integration, and
`WorkflowIntegration.node` are copyright Blackmagic Design Pty Ltd.

This repository does **not** include:

- the Blackmagic OpenFX SDK or Resolve SDK;
- Resolve application binaries;
- `WorkflowIntegration.node` (copy from the user's installed Studio:
  in-app **Help > Documentation > Developer**).

Use of Resolve as a host is governed by the Blackmagic Design end-user
license. Confirm `.node` redistribution limits on a Studio machine
before any installer copies that file (open question in docs/design.md).
